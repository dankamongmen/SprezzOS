# This file is used by l10n-sync to decide whether it should run or not on
# the D-I tree
# If the file contains a line with "run=0", then
# l10n-sync will have no action and will stop immediately after being started
# Otherwise, the script will run 
#
# This can be ignored by using the "--force" switch of l10n-sync
run=1
