diff --git a/debian/changelog b/debian/changelog
index 2a5385d..99679c9 100644
--- a/debian/changelog
+++ b/debian/changelog
@@ -1,3 +1,10 @@
+netcfg (1.86-SprezzOS2) unstable; urgency=low
+
+  * SprezzOS build
+  * Update against debian 1.86
+
+ -- Nick Black <nick.black@sprezzatech.com>  Wed, 08 Aug 2012 09:47:39 -0400
+
 netcfg (1.86) UNRELEASED; urgency=low

  [ Gaudenz Steinlin ]
  * Add logging to iface activation and deactivation.

 -- Christian Perrier <bubulle@debian.org>  Tue, 07 Aug 2012 08:31:59 +0200

 netcfg (1.85) unstable; urgency=low
 
   [ Updated translations ]
diff --git a/debian/netcfg-common.templates b/debian/netcfg-common.templates
index e964a5a..f1fa681 100644
--- a/debian/netcfg-common.templates
+++ b/debian/netcfg-common.templates
@@ -141,7 +141,7 @@ _Description: Failure of key exchange and association
 
 Template: netcfg/get_hostname
 Type: string
-Default: debian
+Default: sprezzbox
 # :sl1:
 _Description: Hostname:
  Please enter the hostname for this system.
diff --git a/netcfg-common.c b/netcfg-common.c
index 133de77..a8f6e79 100644
--- a/netcfg-common.c
+++ b/netcfg-common.c
@@ -925,7 +925,7 @@ int netcfg_get_hostname(struct debconfclient *client, char *template, char **hos
                       "maxhostnamelen", buf);
             debconf_input(client, "high", "netcfg/invalid_hostname");
             debconf_go(client);
-            debconf_set(client, template, "debian");
+            debconf_set(client, template, "sprezzbox");
             free(*hostname);
             *hostname = NULL;
         }
@@ -953,7 +953,7 @@ int netcfg_get_hostname(struct debconfclient *client, char *template, char **hos
                       "maxhostnamelen", buf);
             debconf_input(client, "high", "netcfg/invalid_hostname");
             debconf_go(client);
-            debconf_set(client, template, "debian");
+            debconf_set(client, template, "sprezzbox");
             free(*hostname);
             *hostname = NULL;
         } else {
