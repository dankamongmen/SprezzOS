#!/bin/sh

# growlight-tty is launched from inittab. we chvt over to it, then spin on
# read() so that debconf doesn't continue through the main menu.

# wait on 'read' until we're killed by growlight-wrapper. since read is a shell
# builtin, it can't die independently of us. its signal recipience is our
# receipt of signal.
while true ; do
	chvt 8
	read
done
# SIGUSR1: success
